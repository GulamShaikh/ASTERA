# ASTERA Architecture & Product Decisions

| Date | Decision | Reason | Alternatives | Status |
|---|---|---|---|---|
| 2026-09-17 | Brand name: ASTERA | Distinctive cosmic/exploration direction | NOVA, NOVERA, others | Accepted |
| 2026-09-17 | Tagline: Exploring New Brands. Delivering Quality. | Communicates discovery + quality | Several variants | Accepted |
| 2026-09-17 | Adopted approved logo reference as final ASTERA mark; produced production SVGs (mark, dark-bg lockup, light-bg lockup, favicon) in `brand/logo/` | Owner-approved visual matches the documented design direction; SVG needed for crisp scaling, per-surface recoloring, and favicon use — the PNG references can't do any of that | Keep PNG only (rejected — doesn't scale, can't be recolored per background, poor favicon fidelity) | **Superseded 2026-09-18 (see below)** |
| 2026-09-18 | Rejected the v1 SVG logo variants; deleted `brand/logo/` entirely. `brand/references/astera-logo-master.png` (1:1) is now the approved visual source of truth, replacing the earlier reference PNG. | The v1 SVG geometry was hand-approximated and did not accurately represent the approved mark — owner rejected it on review | Patch the rejected SVGs (rejected — owner wants a rebuild from the corrected master reference, not a patch on wrong geometry) | Accepted; no production SVG exists until a vectorization approach is proposed and approved |
| 2026-09-18 | Adopt a two-layer logo asset strategy: glossy "brand artwork" for hero/marketing/social, separate clean-vector "production logo" for navbar/favicon/print/signage | One asset can't serve both jobs — a glossy render fails at favicon sizes, a flattened vector undersells marketing/hero use | Single universal logo asset (rejected — forces a compromise that hurts either the marketing use or the small-scale production use) | Accepted direction; implementation pending vectorization approval |
| 2026-09-18 | Approved vectorization method: trace `astera-logo-master.png` via Canny edge detection + contour extraction (measured geometry), not freehand approximation. Owner picked this over a zero-dependency manual-measurement-only option and a fully-automated full-image trace. | v1 failed specifically because geometry was estimated from visual impression instead of measured; edge/contour tracing measures the actual pixel boundary instead | Manual pixel-measurement only, no new dependencies (rejected — swoosh curve would still be hand-fitted, the exact failure mode from v1); full automated trace of all shapes (rejected — heavier install for shapes that don't need it) | Accepted; required installing numpy + opencv-python-headless locally (asset-generation tooling only, not a website dependency) |
| 2026-09-18 | Rebuilt v2 production SVGs in `brand/logo/` from the traced geometry; ribbon gradient uses `--cosmic-blue`/`--cosmic-blue-light` by deliberate choice rather than sampled source pixels | Source ribbon has glossy multi-directional 3D lighting (specular highlights), not a simple 2-stop gradient — pixel sampling gave inconsistent, unusable results; the brand's own tokens are more correct than false precision | Attempt to statistically extract gradient stops from source pixels (rejected — tried it, result was incoherent due to specular highlights) | Accepted; light-background colorway still pending sign-off (unchanged from v1) |
| 2026-09-21 | Built the homepage from the Stitch project "ASTERA Tech Retail Homepage" (`projects/17271930474231511185`), screen "v3 Production Ready" — the most recent/refined of the four homepage iterations in that project | Explicitly the newest and only screen titled "Production Ready"; the design system export attached to the project matches DESIGN_SYSTEM.md's palette and extends it with concrete typography/spacing tokens | Earlier screens in the same project ("v2 Online Store", plain "Homepage") — superseded by v3, not used | Accepted |
| 2026-09-21 | Added one new color token, `--color-champagne: #E2C08D`, pixel-sampled from the approved Stitch screenshot's eyebrow-label/badge text | Fulfils CLAUDE.md's "restrained warm gold/champagne accent" requirement, which DESIGN_SYSTEM.md's base 6-token palette has no color for; verified ≥11:1 contrast on both `--midnight` and `--space-black` before adopting | Reuse `--cosmic-blue-light` (rejected — DESIGN_SYSTEM.md restricts it to gradient-highlight use only, never text/surface); leave eyebrows white (rejected — loses the gold accent the brand board calls for) | Accepted |
| 2026-09-21 | Did not add new neutral/border tokens for the slate/border tones the Stitch design system doc lists (e.g. `#1E293B`, `#E2E8F0`, `#94A3B8`) | Tailwind v4's built-in palette already ships `slate-800`, `slate-200`, `slate-400`, `slate-50`, etc. at (or effectively at) those exact hex values, so the utilities already exist without redefining anything | Mirror every Stitch neutral into `tokens.css` as a custom token (rejected — duplicates Tailwind's own palette for no behavioral difference) | Accepted |
| 2026-09-21 | Loaded two Google Fonts — Outfit (headings, weights 500/600) and Plus Jakarta Sans (body, weights 400/500/600) — via a `<link>` in `index.html`, replacing the system-font stack | Matches the approved Stitch design system's typography spec exactly; CLAUDE.md/DESIGN_SYSTEM.md both allow a web font "if using one, load only required weights" | Keep the system-font stack (rejected — approved design specifies these two typefaces); self-host via a new npm package (rejected — a `<link>` costs zero bundle JS, no dependency needed) | Accepted |
| 2026-09-21 | Adapted several literal copy/UI elements from the Stitch v3 screen to comply with CLAUDE.md's content rules, rather than reproducing them verbatim: dropped the claims "100% Genuine", "Fast Dispatch", "7-Day Easy Returns", "ready to order with fast nationwide shipping", and "verified accessories"; dropped the hard-coded "View Cart (2)" header badge and the "In Stock" product badge; replaced the footer's bracketed placeholder email/hours (`[hello@astera.in]`, `[Customer Support: Mon–Sat]`) with a non-committal "Get in Touch" CTA and no invented contact specifics | CLAUDE.md explicitly forbids unsupported claims ("best", "certified", etc. by extension of the same rule) and inventing contact info/hours/staff; a hard-coded cart count and "In Stock" badge would assert real inventory/session state that doesn't exist yet | Ship the literal Stitch copy as-is (rejected — violates the project's content rules); mark every substitution with visible "[placeholder]" bracket text in the UI (rejected — reads as an unfinished/hackathon build, which the client-facing goal explicitly rules out; demo status is instead documented in code comments in `src/data/*.ts`) | Accepted |
| 2026-09-21 | Product/brand demo data uses generic role-descriptor names ("Emerging Brand", "Independent Hardware Studio") instead of invented proper-noun brand names, and omits price entirely (the `Product` type's `price` field is left unpopulated) | CLAUDE.md forbids inventing brands/prices as real business data; ARCHITECTURE.md's own `Product` type already marks `price` optional, and pricing/catalogue is explicitly Phase 4 (future) work, not part of this homepage phase | Invent distinct fictitious brand names for visual variety (rejected — reads as fabricated real business data); show illustrative prices labeled "demo" (rejected — visible "demo" labels undercut the production-quality goal; omitting price sidesteps the conflict entirely) | Accepted |
| 2026-09-21 | No product photography used; product/brand visuals are a category-icon-on-gradient placeholder tile instead of an `<img>` | No real ASTERA product photography exists in the repo; sourcing stock photos to fill the gap would itself be inventing product imagery the brand doesn't own | Use stock photography (rejected — not real ASTERA product imagery, and DESIGN_SYSTEM.md's Photography section prefers real shop/product photography over generic stock); ship broken/empty `<img>` tags (rejected — unpolished) | Accepted; swap in real photography per product when supplied |
| 2026-09-21 | Header CTA row omits the cart and search icons shown in the Stitch reference; primary nav uses in-page anchor links (`#categories`, `#new-arrivals`, etc.) rather than installing a router | ARCHITECTURE.md scopes checkout/cart and multi-page routing as later phases; shipping non-functional cart/search icons or dead-end nav links would misrepresent capability that doesn't exist yet, and a router is unjustified infrastructure for a homepage-only build | Ship the icons as inert/decorative (rejected — non-functional controls read as unfinished); add `react-router` now (rejected — no second page exists yet to route to) | Accepted; revisit both once `/shop` and cart functionality are actually built |
| 2026-09-21 | Superseded the 2026-09-21 "no product photography" decision above: the owner manually exported the actual product photos from the live Stitch v3 screen (browser right-click → Save Image As on each photo) since the MCP's `htmlCode.downloadUrl` sits behind Google auth this session can't reach. 4 of 5 exported files were clean; wired in 3 (hero spotlight/packaging kit shot, GaN charger, braided cable, wireless earbuds) at `public/images/hero/` and `public/images/products/`, added an optional `Product.image` field (falls back to the existing `icon` tile when absent) | These are the actual assets from the approved design, not a fabrication or a screenshot crop — the one thing this correction explicitly required | Crop the individual product photos out of a full-page/section screenshot (rejected — explicitly forbidden by the owner: a screenshot crop isn't the source asset, even if visually identical) | Accepted |
| 2026-09-21 | `magnetic-phone-case` now uses the `phonecase.jpg` export | The export had garbled fake nav-bar text baked into its top ~22px (a Stitch AI-generation artifact). Flagged it and asked before touching the file; owner trimmed the defect strip themselves (in an image editor, 512×279 → 512×257) and dropped the corrected file back into `brand/references/stitch-v3-exports/phonecase.jpg`, which was then synced to `public/images/products/phonecase.jpg`. Verified in-browser: defect gone, all 4 New Arrivals cards now show clean real photography | Use the flawed export as-is (rejected); Claude trims the defect unasked (rejected — asked first since it bordered the screenshot-cropping boundary the owner was explicit about) | Resolved |
| 2026-09-21 | A real Google/Stitch API key was committed in plaintext in `.mcp.json` (discovered via GitHub push protection rejecting the push). Untracked the file, gitignored it, added `.mcp.json.example` as a safe template, and purged the key from all local git history with `git filter-branch` | CLAUDE.md explicitly forbids committing secrets/API keys; the key had to be removed from every historical commit, not just the working tree, before any push could succeed | Leave the key in history and rotate only (rejected — GitHub's scanner would keep blocking every future push referencing that history); force-push without cleaning history (rejected — doesn't remove the exposure) | Accepted; key rotation on the Google/Stitch side is the owner's action, not something this tool can do |
| 2026-09-23 | Backend: chose **Supabase** (Postgres + Auth + Storage) over Firebase (Firestore + Auth + Storage) for the admin-catalogue milestone | ASTERA's data is inherently relational — a product belongs to exactly one brand and one category, each with its own publish/archive lifecycle, and the admin spec needs joins ("products in this category, featured first") and per-row access rules. Postgres + Row Level Security maps directly onto that; Firestore's document model would need denormalization for the same queries, and its security-rules model is a different paradigm than the SQL RLS this schema uses. Supabase also bundles DB + Auth + Storage behind one client library, minimizing integration surface for a single-developer project | Firebase/Firestore (rejected — relational shape doesn't fit a document DB well); a separate DB + separate auth provider (rejected — more moving parts than this project's size justifies) | Accepted |
| 2026-09-23 | Used Supabase's modern browser config (`VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY`, a `sb_publishable_...` key) rather than the legacy anon JWT pattern | Matches the project the owner already created in the Supabase dashboard; functionally equivalent (safe to expose client-side, RLS is still the real boundary) but is Supabase's current recommended default | Legacy `anon` JWT key (rejected — the owner's project was already provisioned with the modern key format; no reason to use the older pattern) | Accepted |
| 2026-09-23 | `brands.category` is a free-text column, not a foreign key to `categories` | The existing `Brand` UI type's `category` field holds descriptive tags like "Audio & Sound" — a different, looser taxonomy than product categories ("Chargers", "Cables"). Forcing it onto the `categories` table would conflate two unrelated concepts just to look more "relational" | FK to `categories` (rejected — no such row would exist for "Audio & Sound"; would require inventing category rows that don't serve the product catalogue) | Accepted |
| 2026-09-23 | `categories.icon_key` stores a string key (`'bolt'`, `'cable'`, ...) resolved to a React icon component via `src/lib/iconKeys.ts`, instead of storing/uploading actual icon assets | The existing icon set (`src/components/common/icons.tsx`) is a small, hand-authored, brand-consistent glyph set — letting admin pick from that fixed set (once the admin UI exists) keeps visual consistency; a database can't store a React component reference directly | Uploadable per-category icon images (rejected — over-engineered for 7 fixed glyphs, and would fragment the icon style); freehand SVG paste field (rejected — same problem, worse) | Accepted |
| 2026-09-23 | `products.brand_id` / `products.category_id` use `on delete restrict`, not `cascade` or `set null` | Normal removal is archive/unpublish, never a hard delete (explicit requirement). A hard delete of a brand/category that still has products should fail loudly and force a deliberate reassignment, not silently orphan or blank out products | `on delete cascade` (rejected — would delete products when a brand is deleted, far too destructive for "curator" data); `on delete set null` (rejected — silently strips a product's brand/category with no warning) | Accepted |
| 2026-09-23 | `profiles` has no INSERT policy reachable from the client; the first (and every) admin account is provisioned manually via the Supabase dashboard + a one-line SQL insert (documented in `supabase/README.md`) | There is no customer-facing sign-up in this project's scope, so any self-serve path into `profiles` would be pure attack surface with no legitimate use | Auto-create a `profiles` row via an `on auth.users insert` trigger (rejected — would let anyone who signs up become a profile row by default, one policy bug away from becoming an admin) | Accepted |
| 2026-09-23 | Seed data links each demo product to the seeded brand whose profile is the closest category match (e.g. wireless earbuds → the audio-focused brand), even though `src/data/products.ts` never encoded a real product→brand relationship (it only used generic descriptors like "Emerging Brand") | `products.brand_id` is a real foreign key now, so seeding needs *some* value; there is no ground truth in the source data to preserve, so the closest defensible mapping was used rather than leaving every product brand-less | Leave `brand_id` null for all seeded products (rejected — makes the seeded Shop page look brand-less/broken for a "curator" site whose whole pitch is brand discovery); invent new brand names to match 1:1 (rejected — fabricates business data) | Accepted; replace with real brand relationships once actual partner brands are onboarded |
| 2026-09-23 | Seed's `product_images.url` still points at the existing `/images/products/*.jpg` files in `public/`, not at the new Supabase Storage buckets | The instruction was to seed the **existing** demo data only; uploading those files into Storage is upload-flow work that belongs to the admin image-management milestone, not this one | Upload the 4 demo images into Storage now and point the seed at those URLs (rejected — scope creep into the next milestone; also nothing in this milestone builds the upload flow to do it through, so it would have to be done by hand, inconsistent with how real uploads will work later) | Accepted; noted as a known gap in `supabase/README.md` |
| 2026-09-23 | Added explicit table `GRANT`s (`20260923050000_grant_table_privileges.sql`) after every catalogue query failed with `42501 permission denied` | RLS controls *which rows* a role sees, but Postgres checks table-level privileges first — both layers must pass. Tables created by raw SQL in the Supabase SQL Editor didn't inherit the default grants to `anon`/`authenticated`. Caught by the browser smoke test, not by typecheck or build | Loosen RLS to work around it (rejected — RLS wasn't the problem, and weakening it would remove the real boundary) | Accepted; the two-layer model is now documented in ARCHITECTURE.md and supabase/README.md |
| 2026-09-23 | `anon` is granted `SELECT` only — no INSERT/UPDATE/DELETE on any catalogue table, and no access to `profiles` at all | Defence in depth: anonymous writes are rejected at the privilege layer (42501) before RLS is even consulted. Verified by attempting anonymous insert/update/delete/profile-read against the live REST API — all five rejected, catalogue unchanged | Grant writes to `anon` and rely on RLS alone (rejected — single layer, and no legitimate anonymous write exists) | Accepted |
| 2026-09-23 | The admin app lives at `/admin/*` in the same Vite project but is **lazily loaded** as separate chunks, with `AuthProvider` mounted only inside that subtree | Public visitors never download admin code and never pay for a Supabase session lookup — the public bundle dropped from 541 kB to 496 kB and admin code splits into per-page chunks. A separate admin app/repo would mean duplicating the design system and deploy pipeline for one small client | Separate admin SPA (rejected — duplicate tooling for a single-developer project); eagerly-bundled admin (rejected — public visitors pay for code they can never use) | Accepted |
| 2026-09-23 | Client-side `ProtectedRoute` treated strictly as UX, never as security | A determined visitor can bypass any React route guard by calling Supabase directly. RLS + table grants are what actually stop them, and those were verified independently against the live API | Rely on route guards and skip RLS verification (rejected — that's the classic "secure-looking but open" failure) | Accepted |
| 2026-09-23 | Category/brand create+edit use a modal form on their list page; only products get dedicated `/new` and `/:id/edit` routes | Matches the agreed route list, and categories/brands have few enough fields to fit a dialog. Products need their own page because image management requires a saved row to attach to | Dedicated routes for all three (rejected — more routes and files than the field count justifies) | Accepted |
| 2026-09-23 | Creating a product redirects to its edit page rather than back to the list | `product_images` rows need a `product_id`, so images can't be uploaded until the product exists. Landing on the edit page puts the image uploader directly in front of the user instead of making them find the product again | Return to the list after create (rejected — hides the next obvious step); upload images to a temp location and move them on save (rejected — far more machinery for the same result) | Accepted |
| 2026-09-23 | Product images save immediately (upload/reorder/delete), separately from the form's Save button | Storage uploads and `product_images` rows are their own transactions; batching them into form submit would mean holding files in memory and risking a partial save. The UI states this explicitly so the split isn't surprising | Defer all image writes to form submit (rejected — complex, and a failed save would strand uploaded files) | Accepted |
| 2026-09-23 | Archiving a category or brand first counts the products referencing it and names that number in the confirmation | `products.category_id`/`brand_id` use `on delete restrict`, but archiving isn't deleting — it silently leaves published products pointing at a hidden page. The admin should see that before confirming, not discover it on the public site | Block archiving while products reference it (rejected — too rigid; sometimes hiding a category with live products is intended); archive silently (rejected — that's the "silently break relationships" failure the brief warns about) | Accepted |
| 2026-09-23 | Deleting an image removes both the database reference and the Storage object, and a missing Storage object never blocks clearing the reference | Otherwise every delete leaves one of the two behind — either an orphaned file nobody can see or a broken `<img>`. The seeded demo images live in `/public`, not Storage, so the helper detects a non-Storage URL and skips the object delete instead of erroring | Delete only the row (rejected — orphaned files accumulate in the bucket); fail the whole delete if the object is missing (rejected — leaves an unfixable broken reference) | Accepted |
| 2026-09-23 | Built `src/lib/api/*` (products/categories/brands, all published-only) but did **not** switch `Home.tsx`/`Shop.tsx` over to use it yet | Explicit instruction: "Do NOT remove the existing demo data yet." The API layer is written and its shape verified to match the existing `Product`/`Category`/`Brand` UI types exactly, so the swap is a data-source-only change with no component edits when it happens | Wire the pages now since the layer is ready (rejected — direct contradiction of the given instruction) | Accepted; next milestone |
| 2026-09-24 | The client-provided number (`9372194085`) populates both `WHATSAPP_NUMBER` and the Contact page's "Phone" channel (`tel:` link), rather than leaving Phone pending | The brief introduced the number as "client whatsapp" but then, in the Contact section, recapped it under a bare "phone number" bullet alongside email/address/hours as "the info I provided" — read together as one number serving both purposes, which is the common pattern for a small business. Trivially correctable: split them in `src/content/site.ts` if the client actually uses a separate landline | Leave "Phone" pending since only a WhatsApp number was explicitly labelled as such (rejected — makes the Contact page look one field short for no real reason, given the more natural reading of the brief) | Accepted; confirm with client |
| 2026-09-24 | `src/lib/whatsapp.ts` assumes a bare 10-digit number is Indian and prepends `91` before building the `wa.me` link | wa.me requires full international digits; the client supplied a plain 10-digit number and ASTERA's given address is in Mumbai | Require the number pre-formatted with country code (rejected — an easy way to accidentally ship a broken link if whoever edits `site.ts` later doesn't know that convention) | Accepted |
| 2026-09-24 | Product page's WhatsApp CTA sits directly under price/description (before Specifications/Compatibility), not after them | The milestone brief lists page content in the order Name → Brand → Price → Stock → Description → Specifications → Compatibility → CTA, but also separately calls out "CTA visibility" as a mobile priority since WhatsApp is the entire business flow. Keeping the button in the first viewport (no scroll past two more sections) serves that priority; nothing in the content list is omitted, just the actionable step isn't buried under it | Move the CTA to the literal end of the section, after Compatibility (rejected — burying the one action that drives the business under specs a browsing visitor may not read) | Accepted |
| 2026-09-24 | No Privacy Policy / Terms / Return Policy pages added. Footer instead gained one factual line: "ASTERA curates and sells accessories from independent and emerging brands. Product names, logos, and trademarks belong to their respective owners." | CLAUDE.md and this milestone both forbid inventing legal text (returns, warranties, shipping promises). That one line is a factual statement about the business model (marketplace/curator, not manufacturer), not legal advice, and directly addresses the trademark/liability concern raised — real policy pages need an actual legal review before the client takes real orders | Write full Privacy/Terms/Returns pages with placeholder legal text (rejected — explicitly forbidden, and fabricated legal text is worse than none); add nothing (rejected — leaves the trademark/liability concern unaddressed) | Accepted; recommend the client get real policy pages drafted before accepting payments |
| 2026-09-24 | Removed `Contact.tsx`'s `?product=` query-param flow (the `EnquiryContext` component reading `useSearchParams`) | Product enquiries now go straight to WhatsApp from the product page with the product name embedded in the message text itself — nothing links to `/contact?product=slug` anymore, so the code was dead | Keep it as a fallback entry point (rejected — unreachable dead code with no live link pointing at it) | Accepted |
| 2026-09-24 | Header lockup rebuilt as the approved `astera-logo-mark.svg` (geometric mark only) beside real HTML text "ASTERA" (`font-heading`), instead of the previous stacked mark+text `dark-bg` SVG at header height | At `h-9`, the stacked SVG's baked-in wordmark text rendered too small to read, looking like a mark-only logo. No horizontal (mark-beside-word) SVG asset exists yet; composing the approved mark asset with real text is the standard way to build a header lockup and doesn't hand-draw the mark itself | Keep the stacked `dark-bg` SVG larger (rejected — grows the whole lockup, including the mark, just to make baked-in text legible); commission a new horizontal lockup SVG (rejected — asset work out of scope for a spacing/polish pass) | Accepted; revisit if a proper horizontal lockup SVG is ever produced |
| 2026-09-24 | Hero's cosmic orbit rings + sparkle use plain CSS `@keyframes` classes in `global.css` (`.orbit-ring`, `.orbit-ring-reverse`, `.sparkle`) applied to inline SVG/icon elements, and are hidden below `lg` | No animation library needed — matches the existing global `prefers-reduced-motion` rule (already forces `animation-duration`/`iteration-count` to near-zero for `*`), so reduced-motion support required no new code. Hidden below `lg` rather than rescaled, since the two-column desktop layout the rings are positioned against collapses to a single stacked column on mobile | A JS animation library (rejected — CSS keyframes are sufficient and CLAUDE.md disallows unnecessary dependencies); rescale rings for mobile instead of hiding (rejected — more code for a decorative element the brief explicitly allows to be simplified away on small screens) | Accepted |
| 2026-09-24 | **Supersedes the "hidden below `lg`" part of the row above.** Orbit rings now sit in a layer centred on the spotlight card (sized as a % of the card column) and render at every breakpoint; the section's `overflow-hidden` + `isolate` clips them so they cannot cause horizontal scroll. Reduced-motion now sets `animation: none` on the decorative classes (fully static) instead of relying on the global near-zero-duration rule | Final showcase polish asked for rings visibly *behind the spotlight* and a perceptible orbit; anchoring to the card keeps them aligned at every width instead of drifting relative to the section. Travelling dots on each ring make the slow rotation perceptible without speeding it up | Keep rings section-anchored and desktop-only (rejected — misaligned with the card on wide screens, no atmosphere on mobile) | Accepted |
| 2026-09-24 | Added `Starfield` (seeded twinkling stars + optional comet, pure CSS keyframes) to Hero, Brand Discovery and Final CTA | Owner asked for visible cosmic motion on the homepage background. A seeded PRNG keeps the sky identical on every visit (a designed composition). Only opacity/transform animate (compositor-friendly); some stars stay still to avoid visual noise. **Reduced motion (revised same day):** anything that moves (orbit rotation, comet, turning glints) stops, but opacity-only twinkling keeps running as a slow 6s fade — a fade is not motion, and freezing everything made the site look dead on machines with Windows "Animation effects" off (the owner's own PC) | Canvas/JS particle system (rejected — per-frame JS for decoration, heavier, harder for the owner to maintain) | Accepted |
| 2026-09-24 | Homepage "View All …" links moved from beside the section eyebrow to a centred button under each grid (Categories' removed entirely per owner request) | Beside the small eyebrow they read as stray, misplaced links; under the grid they are a clear next step after browsing | Delete all three (rejected — loses the path from homepage to full Shop/Brands listings) | Accepted |
| 2026-09-24 | Product cards follow the Stitch catalogue card (solid `surface` card, square image, price left + solid Cosmic Blue "View Details" right). No placeholder prices were added in code | Prices come from Supabase and are set per product in Admin → Products; hard-coded dummy prices would be ignored by the live data and risk a fake price reaching a client. Unpriced products show "Price on enquiry" (same wording as the product page) | Seed dummy prices (rejected — CLAUDE.md forbids invented prices; admin is the correct place) | Accepted |
| 2026-09-24 | Footer gained a discreet admin entry point — revised same day from an "Admin Login" text link to a faint lock icon (tooltip + aria-label "Admin sign in") linking to `/admin`, which redirects to login when signed out. Owner felt the text link looked out of place on a storefront | The owner needs a visible entry point when demoing the CMS to clients. Hiding the URL is not security — access is enforced by Supabase Auth + RLS | No link, rely on typing the URL (rejected — owner could not find it) | Accepted; remove the link later if the client prefers |
| 2026-09-24 | Added `vercel.json` rewrite + `public/_redirects` (Netlify) sending all routes to `index.html` | React Router is client-side; without a rewrite, refreshing or opening a shared deep link like `/product/…` returns the host's 404. Both files are inert on the other host, so hosting stays a free choice | Hash routing (rejected — ugly URLs for a showcase site) | Accepted |

| 2026-09-25 | Homepage's Curated Online Catalog, Discovery Grid, and Brand Discovery sections become a horizontally swipeable card rail (`layout="rail"` on ProductGrid/CategoryGrid/BrandGrid) below `sm`, reverting to the existing CSS grid from `sm`/`md` up. New shared `scrollRail.ts` classes; `layout` defaults to `'grid'` so Shop, category/brand detail, and related-product listings are unaffected | Owner reported real phone screenshots: full-width square product photos stacked one per row made the homepage feel oversized and endlessly scrolly. A narrower rail card shrinks the image proportionally and shows the next card peeking in, communicating "swipe" without extra UI chrome (dots/arrows), matching CLAUDE.md's "avoid clutter" | Shrink the image aspect ratio instead of changing layout (rejected — departs from the approved Stitch card design, and doesn't fix "everything stacks vertically"); apply the rail to `/shop` too (rejected — that page has search/filter/sort and is meant for exhaustive vertical browsing, not a quick swipeable teaser) | Accepted |
| | | | | |

## Rule
Do not silently reverse an important decision.
If a major change becomes necessary:
1. explain why,
2. identify impact,
3. update this file.
