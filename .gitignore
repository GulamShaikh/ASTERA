# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables / secrets
.env
.env.*
!.env.example

# MCP config holds real API keys locally — never commit it
.mcp.json
.mcp.json.example

# Claude Code project files and internal process/planning docs — kept locally
# for continued development, not published to the client-facing repo
.claude/
CLAUDE.md
docs/BUILD_ROADMAP.md
docs/DECISIONS.md
docs/LEARNING_LOG.md

# Raw design-exploration exports (kept locally in case the logo needs
# reworking later) — brand/logo/ (final source SVGs) stays tracked
brand/references/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
